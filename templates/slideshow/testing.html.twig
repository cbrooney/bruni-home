{% extends 'base.html.twig' %}

{% block body %}
    <div class="rahmen" id="app-rahmen">
        <img src="bilder/DSCF0879.JPG" alt="Snow" class="einzelnesBild">
{#        <div class="top-left">Top Left</div>#}
        <div id="filename">
            Dateiname:
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script
            src="https://code.jquery.com/jquery-3.6.0.min.js"
            integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
            crossorigin="anonymous">
    </script>

    <script type="text/javascript">
        function toggleFullscreen() {
            // let rahmenId = 'app-rahmen';
            // let rahmen = document.getElementById(rahmenId);

            let rahmenId = 'app-rahmen';
            let rahmen = document.getElementById(rahmenId);

            document.onclick = (event) => {
                if (document.fullscreenElement) {
                    rahmen.exitFullscreen()
                        .then(() => console.log("Document Exited from Full screen mode"))
                        .catch((err) => console.error(err))
                } else {
                    if (rahmen.requestFullscreen) {
                        rahmen.requestFullscreen();
                    } else if (rahmen.webkitRequestFullscreen) { /* Safari */
                        rahmen.webkitRequestFullscreen();
                    } else if (rahmen.msRequestFullscreen) { /* IE11 */
                        rahmen.msRequestFullscreen();
                    }
                    // document.documentElement.requestFullscreen();
                }
            }



            // if (rahmen.requestFullscreen) {
            //     rahmen.requestFullscreen();
            // } else if (rahmen.webkitRequestFullscreen) { /* Safari */
            //     rahmen.webkitRequestFullscreen();
            // } else if (rahmen.msRequestFullscreen) { /* IE11 */
            //     rahmen.msRequestFullscreen();
            // }
            //
            // document.getElementById("filename").innerHTML = '<h1 id="filename">Massimo' + '</h1>';
        }


        $(document).ready(function() {
            document.onclick = (event) => {
                if (document.fullscreenElement) {
                    document.exitFullscreen()
                        .then(() => console.log("Document Exited from Full screen mode"))
                        .catch((err) => console.error(err))
                } else {
                    let rahmenId = 'app-rahmen';
                    let rahmen = document.getElementById(rahmenId);
                    rahmen.requestFullscreen();
                }
            }
        });

        function openFullscreen() {
            let rahmenId = 'app-rahmen';
            let rahmen = document.getElementById(rahmenId);

            if (rahmen.fullscreenElement) {
                rahmen.exitFullscreen()
                    .then(() => console.log("Document Exited from Full screen mode"))
                    .catch((err) => console.error(err))
            } else {
                if (rahmen.requestFullscreen) {
                    rahmen.requestFullscreen();
                } else if (rahmen.webkitRequestFullscreen) { /* Safari */
                    rahmen.webkitRequestFullscreen();
                } else if (rahmen.msRequestFullscreen) { /* IE11 */
                    rahmen.msRequestFullscreen();
                }

                document.getElementById("filename").innerHTML = '<h1 id="filename">Massimo' + '</h1>';
                // rahmen.documentElement.requestFullscreen();
            }
        }
    </script>
{% endblock %}

{% block stylesheets %}
    <style>
        *:fullscreen, *:-webkit-full-screen, *:-moz-full-screen {
            background-color: rgba(255,255,255,0)!important;
            padding: 20px;
        }

        ::backdrop
        {
            background-color: white;

        }
        /* Container holding the image and the text */
        .rahmen {
            position: relative;
            text-align: center;
            color: white;
        }

        /* Top left text */
        .top-left {
            position: absolute;
            top: 8px;
            left: 16px;
            color: black;
        }

        #filename {
            position: absolute;
            top: 8px;
            left: 16px;
            color: black;
        }

        .einzelnesBild
        {
            /*display:flex;*/
            /*justify-content: center;*/
            /*align-items: center;*/
            /*margin-left: auto;*/
            /*margin-right: auto;*/
            /*margin-top: auto;*/
            /*z-index:0;*/
            /*width: 100%;*/
            /*height: 100vh;*/
            max-width: 100%;
            max-height: 100%;
            height: inherit !important;
            /*display: none;*/
        }
    </style>
{% endblock %}